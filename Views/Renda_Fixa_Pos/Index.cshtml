@model IEnumerable<ControlCash.Models.Renda_Fixa_Pos>

@{
    ViewBag.Title = "Index";
}

<h2>Renda Pós Fixada</h2>

<p>
    @Html.ActionLink("Novo Registro", "Create", null, new { @class = "btn btn-success btn-custom" })
</p>
<table class="table table-striped">
    <tr>
        <th>
            @Html.DisplayName("Instituição Financeira")
        </th>
        <th>
            @Html.DisplayName("Emissor")
        </th>
        <th>
            @Html.DisplayName("Papel")
        </th>
        <th>
            @Html.DisplayName("Indexador")
        </th>
        <th>
            @Html.DisplayName("Porcentagem")
        </th>
        <th>
            @Html.DisplayName("Data de Início")
        </th>
        <th>
            @Html.DisplayName("Data de Vencimento")
        </th>
        <th>
            @Html.DisplayName("Valor Aplicado")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Instituicao.nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Emissor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Papel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Indexador)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Porcentagem)
        </td>
        <td>
            @{
                String data = item.Data_Inicio;
                data = data.Substring(8, 2) + "/" + data.Substring(5, 2) + "/" + data.Substring(0, 4);
            }
            @data
        </td>
        <td>
            @{
                data = item.Data_Venc;
                data = data.Substring(8, 2) + "/" + data.Substring(5, 2) + "/" + data.Substring(0, 4);
            }
            @data
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Valor_Aplicado)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.Renda_PosID }, new { @class = "btn btn-warning btn-custom" })
            @Html.ActionLink("Excluir", "Delete", new { id=item.Renda_PosID }, new { @class = "btn btn-danger btn-custom" })
        </td>
    </tr>
}

</table>
